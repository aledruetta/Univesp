export class Escritor {
  id: number;
  nombre: string;
  pais: string

  constructor(id: number, nombre: string, pais: string) {
    this.id = id;
    this.nombre = nombre;
    this.pais = pais;
  }
}
